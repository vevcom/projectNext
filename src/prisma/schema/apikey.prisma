model ApiKey {
  id               Int          @id @default(autoincrement())
  name             String       @unique
  keyHashEncrypted String       @unique
  active           Boolean
  permissions      Permission[]
}
