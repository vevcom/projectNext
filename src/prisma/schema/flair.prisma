model Flair {
  id      Int       @id @default(autoincrement())
  name    String
  image   Image @relation(fields: [imageId], references: [id])
  imageId Int @unique

  User User[] @relation(name: "userFlairs")
}
