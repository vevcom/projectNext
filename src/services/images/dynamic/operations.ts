import { defineOperation } from '@/services/serviceOperation'


export const dynamicImages = {
    readDynamicImageCollectionPage: defineOperation({
        operation: async ({  }) => {

        }
    })
} as const