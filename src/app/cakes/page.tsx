import PageWrapper from '../_components/PageWrapper/PageWrapper';
import styles from './page.module.scss'
export default async function cakes() {
    return (
        <PageWrapper title='Kaker'>
            
            Kake er godt
        </PageWrapper>
    );
}