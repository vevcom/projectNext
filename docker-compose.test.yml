services:
    projectnext:
        extends:
            file: docker-compose.base.yml
            service: projectnext
        build:
            target: test
        tty: true # Allows for colorized output
    
    db:
        extends:
            file: docker-compose.base.yml
            service: db
        ports:
            - 5432:5432
