import { describe, expect, test } from '@jest/globals';

describe("Dots", () => {
    test("should return a list of dots", async () => {

    })

    test("should return a dot by id", async () => {
    
    })

    test("should return a list of dots by user id", async () => {
    
    })
})